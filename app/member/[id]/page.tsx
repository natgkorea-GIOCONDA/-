'use client';
import { useEffect, useState } from 'react';import Image from 'next/image';import { AuthGate } from '@/components/AuthGate';import { Header } from '@/components/Header';import { createClient } from '@/lib/supabase';import type { Member } from '@/lib/types';
export default function Detail({params}:{params:{id:string}}){const [m,setM]=useState<Member|null>(null);useEffect(()=>{createClient().from('members').select('*').eq('id',params.id).eq('is_visible',true).eq('privacy_consent',true).single().then(({data})=>setM(data))},[params.id]);return <AuthGate><Header/><main className="mx-auto max-w-2xl px-4 py-6">{m&&<section className="overflow-hidden rounded-3xl bg-white shadow-card"><div className="bg-navy p-6 text-white"><div className="relative h-32 w-32 overflow-hidden rounded-3xl bg-white/10">{m.photo_url&&<Image src={m.photo_url} alt={m.name} fill className="object-cover"/>}</div><p className="mt-4 text-gold">CEO과정 {m.cohort}기</p><h1 className="text-3xl font-extrabold">{m.name}</h1><p>{m.company_name} · {m.position}</p></div><dl className="grid gap-4 p-6 text-lg">{[['주소',m.company_address],['이메일',m.email],['전화',m.phone],['웹사이트',m.website],['업종',m.industry],['사업유형',m.business_type],['회사소개',m.company_intro]].map(([k,v])=><div key={k}><dt className="text-sm font-bold text-gold">{k}</dt><dd className="whitespace-pre-wrap text-charcoal">{v||'-'}</dd></div>)}</dl></section>}</main></AuthGate>}
