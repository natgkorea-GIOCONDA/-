import Link from 'next/link';import Image from 'next/image';import type { Member } from '@/lib/types';
export function MemberCard({m}:{m:Member}){return <article className="rounded-3xl border border-slate-200 bg-white p-4 shadow-card"><div className="flex gap-4"><div className="relative h-20 w-20 shrink-0 overflow-hidden rounded-2xl bg-slate-100">{m.photo_url?<Image src={m.photo_url} alt={m.name} fill className="object-cover"/>:<div className="grid h-full place-items-center text-2xl font-bold text-navy">{m.name[0]}</div>}</div><div className="min-w-0 flex-1"><p className="text-xs font-semibold text-gold">CEO과정 {m.cohort}기</p><h3 className="truncate text-xl font-bold text-navy">{m.name}</h3><p className="truncate font-semibold text-charcoal">{m.company_name}</p><p className="truncate text-sm text-slate-500">{m.position} · {m.industry}</p></div></div><Link href={`/member/${m.id}`} className="mt-4 block rounded-2xl bg-navy px-4 py-3 text-center font-semibold text-white">상세보기</Link></article>}
